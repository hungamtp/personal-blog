create table post
(
    id        number not null,
    title     varchar2(255 char),
    content   varchar2(255 char),
    createdAt timestamp,
    editedAt  timestamp,
    publish   number(1, 0),
    primary key (id)
);

create table post
(
    id        number not null,
    title     varchar2(255 char),
    content   varchar2(255 char),
    createdAt timestamp,
    editedAt  timestamp,
    publish   number(1, 0),
    primary key (id)
);
